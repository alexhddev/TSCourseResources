

const concatenateStrings = (arg1, arg2) =>{
    return arg1 + arg2
}

const concatenateStringsWithSpace = (arg1, arg2) =>{
    return arg1 + ' ' +arg2
}

module.exports = {
    concatenateStringsWithSpace,
    concatenateStrings};