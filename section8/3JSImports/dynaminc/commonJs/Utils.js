

const concatenate = (arg1, arg2) =>{
    return arg1 + arg2
}

const concatenateWithSpace = (arg1, arg2) =>{
    return arg1 + ' ' +arg2
}

module.exports = {
    concatenateWithSpace,
    concatenate};